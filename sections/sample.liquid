<h2>{{ section.settings.text-input | escape }}</h2>

{% schema %}
  {
    "name": "サンプルセクション",
    "settings": [
      {
        "type": "text",
        "id": "text-input",
        "label": "テキスト入力",
        "default": "ここにテキストを入力する"
      }
    ],
    "presets": [
      {
        "name": "サンプルセクション"
      }
    ]
  }
{% endschema %}